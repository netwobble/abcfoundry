import { CockpitStrategyModule } from './cockpit-strategy.module';

describe('CockpitStrategyModule', () => {
  it('should work', () => {
    expect(new CockpitStrategyModule()).toBeDefined();
  });
});
